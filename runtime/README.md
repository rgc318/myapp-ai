# Runtime data

`runtime/governance-reports/` is mounted read-only into the standalone Orchestrator. Copy only approved, redacted live-gate and Embedding release reports here. Runtime reports, secrets, snapshots and backups are ignored by Git.
